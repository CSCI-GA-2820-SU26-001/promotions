apiVersion: v1
kind: Service
metadata:
  name: promotionservice
  labels:
    app: promotionservice
spec:
  selector:
    app: promotionservice
  type: ClusterIP
  internalTrafficPolicy: Cluster
  ports:
    - name: 8080-tcp
      protocol: TCP
      port: 8080
      targetPort: 8080
